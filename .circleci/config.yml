version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:12.9.1-browsers

    working_directory: ~/repo

    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-
      - run: npm install
      - add_ssh_keys:
          fingerprints:
            - "2f:7f:65:73:50:f2:06:3c:2b:6f:54:0a:32:20:ab:c2"

      - run:
          name: Clone react repo 
          command: |
            git clone https://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@github.com/punit1109/punit-private.git
            pwd

      - save_cache:
          paths:
            - node_modules
            - ~/.npm
            - ~/.cache
          key: v1-dependencies-{{ checksum "package.json" }}

      - run: npm test

      - run: npm build
    